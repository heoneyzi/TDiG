# GDTR 코사인 렌즈 — 실험 1~10 전체 총평: 왜 코사인이며, 왜 32-레이어 누적이 유효한가

본 문서는 실험 1~10의 **실측 결과만을 근거로**, 세 가지를 논리적 빈틈 없이 정리한다.

1. **왜 코사인(direction-only) 렌즈를 쓰는 것이 유용하고 합리적인가?**
2. **왜 32개 레이어에 걸친 "레이어별 차이(누적)"로 측정/학습하는 것이 유효한가?**
3. 각 실험의 **필요성 · 목적 · 성과**, 그리고 **아쉬운 점과 실험 10의 발전 방향**.

모든 수치는 실제 실행 출력(`results/wcl/…`, `results/wcl_r1/…`, 실험 10 real run)에서 인용했다. 유리한 증거만 모으지 않으며, 약하거나 미완인 부분(§8)을 함께 밝힌다. (실험 8은 메모리로 미수행이나, 그 목표는 실험 10이 인과 버전으로 상당 부분 대체했다 — §2·§7 참조.)

---

## 0. 한 문장 결론

> 코사인은 "가장 잘 분류하는 렌즈"라서가 아니라, **(1) 모델이 실제로 읽는 양(방향)** 을, **(2) 모델 자신의 최종 정답 프레임 `h_norm`** 을 기준으로, **(3) 유계·well-posed한 정착(settling) 구성**으로 재는 유일한 렌즈이고, 그 정착이 일어나는 **레이어 인덱스 자체가 통계적으로도 인과적으로도 실재하는 신호**(레이어를 섞으면 성능이 무너지고, 크기는 한 층에 몰린 단일 스칼라인 반면 코사인은 32층에 분산된 정보)이기 때문에 합리적이다. 실험 10은 여기에 **"모델 계산이 크기가 아니라 방향에 인과적으로 133배 더 민감하고, 그 인과 구조가 오직 `h_norm` 기준에서만 나타난다"** 를 더해, "왜 코사인·왜 `h_norm`"을 인과적으로 뒷받침한다.

---

## 1. 우리가 증명하려는 주장 (논리 구조)

GDTR의 구성물(construct)은 "변이 병원성 분류기"가 아니라 **"한 토큰의 표현이 모델의 출력 준비 프레임에 언제(어느 레이어에서) 도달하는가 = 정착 깊이 c(t)"** 이다. 이 정의가 렌즈의 요건을 강제한다.

| 요건 | 내용 | 검증 실험 |
|---|---|---|
| R1 유계(bounded) | 임계값 γ로 "도달"을 판정하려면 거리가 유계여야 함 | 1, 6 |
| R2 참조 고정 | "무엇에 도달하는가"의 기준 = 언임베딩이 읽는 `h_norm` | 7, **10** |
| R3 well-posed | 정착 깊이가 토큰마다 변해야(내용 의존) 의미 | 1, 6, 7, 9 |
| R4 레이어 유의미 | "어느 레이어"가 정보를 담아야 층위별 측정이 정당 | 9, 10 |

이로부터 **5겹 주장**이 선다(각 실험이 어느 주장을 받치는지 명시).

- **A. 구성 필요성** — 크기/거리 렌즈는 R1·R3을 위배(축퇴·깊이지배) → 실험 1, 2, 6, 7A.
- **B. 분업(경쟁 아님)** — 크기는 "얼마나 교란됐나(scoring)", 방향은 "어디서 커밋하나(localization)" → 실험 3b, 9.
- **C. 방향의 인과 지배** — 동일 예산 출력공간에서 방향이 크기를 압도 → 실험 4v2, **10(133×)**.
- **D. 참조(`h_norm`) 특이성** — 신호가 오직 `h_norm` 기준에서만 → 실험 7(암시·부분순환) + **10(인과적으로 `h_norm`에서만)**.
- **E. 레이어 유효성** — 층위별 코사인 차이가 실재·순서의존·라벨결부 → 실험 9.

---

## 2. 실험별 필요성 · 목적 · 성과 (실측)

### 실험 1 — 크기/거리 축퇴 배터리
- **필요성**: "왜 크기(magnitude)를 쓰지 않았나"에 답해야 한다(크기는 직관적이고, 논문 Appendix C에서 크기 특징이 코사인을 이긴 적도 있음).
- **목적**: 크기·거리 렌즈를 코사인과 **동일한 정착 파이프라인**에 넣어 정착 구성으로서 성립하는지 본다.
- **진행 방법**: TDiG의 per-token 정착 테이블(`chr22_tier1.parquet`, 12,978 윈도우)에서 **코사인 셀 `M1_dir_refC`** 와 **크기 셀 `M2_mag_refA`** 를 읽는다 — 두 셀 모두 **같은 running-min + 지속성 W=3 + q70 보정** 규칙으로 계산된 값이라 비교가 공정하다. (a) 전 위치의 정착 깊이로 평균·표준편차·**변동계수(CV)**·상위 2층 포화율을 두 셀에 대해 계산해 대조하고, (b) 윈도우 메타데이터의 엑손/인트론 비율로 맥락을 나눠 **엑손 vs 인트론 Cohen's d + Mann-Whitney U** 로 두 셀의 생물학 신호를 비교한다.
- **성과**: 크기 정착은 **전 토큰의 100%가 상위 2개 층(28–29)에 몰리고**(CV=0.017, 거의 상수), 생물학 신호 없음(엑손 vs 인트론 d=+0.11, p=0.09 비유의). 코사인은 분산(CV=0.246)·강한 생물학(d=−0.345, p=3×10⁻⁸). **CV 비율 14.7×**, M2(크기)·M4(화이트닝)는 3개 참조 중 2개에서 완전 축퇴. → **크기는 ill-posed(거의 상수), 코사인은 well-posed.** (주장 A)

### 실험 2 — 로그 차원(rogue dimension) 강건성
- **필요성**: 코사인이 "소수의 이상(outlier) 차원"에 휘둘리는 인공물일 수 있다(Timkey & van Schijndel 2021; 논문 G.1이 스스로 제기).
- **목적**: 각 층에서 상위 k개 차원이 분산을 얼마나 차지하는지, 표준화 후에도 정착이 유지되는지 본다.
- **진행 방법**: HF의 **원시 은닉상태 `chr22_tier3_raw.h5::raw_h_ell`(100 윈도우×32층×600토큰×4096차원)** — 원시 차원값이 필요하므로 스칼라 캐시가 아니라 이 파일을 쓴다 — 에서, 층마다 4096차원 각각의 분산을 구해 **상위 1/8/64 차원이 차지하는 비율**과 공분산 고유스펙트럼(등방성·유효랭크)을 계산한다. 이어 **같은 은닉상태로 코사인 정착을 세 방식으로 재계산**해 비교한다: (i) 원시, (ii) 차원별 z-표준화 후, (iii) 상위 64차원 제거 후. 세 방식의 정착을 **Spearman 일치**와 **splice(도너 vs 인트론) Cohen's d** 로 대조한다(위치 라벨은 gDTR `prep_chr22_windows.py` 산출물을 token_stride에 맞춰 사용).
- **성과(부분)**: 정준 탭 **L29에서 상위 8개 차원 = 분산의 0.76%**(깨끗). **그러나 중간층 L15에서 상위 8개 = 96%, L24 = 90%** 로 소수 차원이 극단 지배. L30/31은 분산 1.3×10²⁵(크기 폭발). **표준화·top-k 제거 후 정착 유지 검증(H2b/H2c)은 미수행**. → "로그 차원 없음"은 **L29에서만 참**이며 중간층 강건성은 미결(§8 결함).

### 실험 3b — 국소화(localization) vs 분류(scoring)
- **필요성**: 리뷰어의 최난문 — "크기 특징이 분류에서 코사인을 이기는데 왜 코사인?"
- **목적**: 변이 은닉상태(HF)로 per-layer ΔD_cos vs ‖Δh‖의 AUROC·상보성·argmax 구조를 직접 비교.
- **진행 방법**: HF의 **`variant_h_ell_{ref,alt}.h5`(10,910 변이×32층×4096)** 에서 참조/대립 은닉상태를 읽어 층별로 세 특징을 직접 계산한다 — **방향거리 ΔD_cos**, **순수 크기변화 Δmag**, **총 ‖Δh‖**. 8,008개 P/LP vs B/LB 변이에 대해 **`StandardScaler → LogisticRegression`, 10-fold 층화 CV** 로 세 특징집합의 AUROC를 구한다: **D(32-d) / M(32-d) / D+M(64-d)**. **1,000회 부트스트랩**으로 상보성(D+M − M, D+M − D)의 신뢰구간을 검정하고, 각 변이의 **argmax 층(최대 교란 층) 분포**로 국소화 구조를 비교한다. 저장된 `variant_scalars.parquet`의 `delta_h_norm_2`와 행 정렬을 Spearman으로 교차검증(=1.0).
- **성과**: AUROC(32-d) **코사인 0.938 ≈ 크기 0.935**, 결합 0.945(정렬검증 ρ=1.0). 상보성: D+M−M=+0.010, D+M−D=+0.0065(둘 다 CI 0 제외) → 서로 정보를 더한다. argmax는 코사인이 분산(평균 5.8/std 10.9), 크기는 최빈 0층(65%). → 논문의 "크기≫코사인" 긴장은 **이 파이프라인에선 재현 안 됨**; 방어는 "동률이며 상보적 — 코사인=국소화, 크기=스코어링"으로 선다. (주장 B)

### 실험 4 v2 — 방향/크기 분리 인과 패칭 (출력공간·동일예산·null)
- **필요성**: 앞선 실험은 상관·서술적. **모델의 실제 계산이 방향에 더 민감한가**를 인과적으로 물어야 한다. (초기 버전은 은닉상태 L2 지표라 교란 크기와 인과 중요도가 뒤섞였음 → v2로 교정.)
- **목적**: 같은 크기 B의 개입을 방향(각도)·크기(반경)·null(무작위 on-manifold)로 주고 **다음 토큰 분포의 KL**을 잰다.
- **진행 방법**: gDTR 경로로 **Evo-2를 forward하며 각 블록에 write hook** 을 건다. 30개 P/LP + 30개 B/LB 변이를 뽑아 참조/대립 서열을 참조 유전체(chr2·3·5·7·10·11·12·13·16·17)에서 재구성해 은닉상태를 얻고, 층 **{12,20,24,27}** 에서 **동일 크기 B=‖h_ref−h_alt‖** 의 개입 네 종을 준다: **방향**(alt를 ref 방향으로 크기 B만큼 회전 이동), **크기**(alt 자기 방향으로 반경 B만큼 이동), **null**(같은 층 다른 실제 위치 상태 쪽으로 B만큼 이동 = on-manifold 바닥), **full**(=ref로 교체). 각 개입 후 변이 위치 + 하류 몇 위치에서 **다음 토큰 분포의 대칭 KL(출력공간)** 을 측정하고, 변이별로 **방향 vs 크기를 짝지어 Wilcoxon** 검정하며 **방향 > null** 여부를 함께 본다. (초기 버전의 은닉상태-L2 지표를 출력공간 KL로 교정한 것이 v2.)
- **성과**: 층별 방향/크기 = **40~145×**, 모든 층에서 방향>null, 크기≤null(인과적 무력). → **동일 예산·출력공간에서 방향이 인과적으로 지배** = "방향은 더 큰 교란이라서"가 아닌 진짜 인과. (주장 C) 단 n=23(소표본), 절대효과 완만.

### 실험 5 — 교차 아키텍처
- **필요성**: 위 현상이 Evo-2만의 우연인지, 잔차 스트림의 일반 성질인지.
- **목적**: HyenaDNA 등에서 재현되는지.
- **진행 방법**: 실험 1의 `results/splice_vs_intron.csv`에서 **Evo-2의 코사인·크기 셀 d** 를 읽고, TDiG가 이미 돌린 `results/hyenadna_crossarch/`(`comparison_vs_evo2.json`, `hyenadna_tier1.parquet`)에서 **HyenaDNA의 곡률(궤적) splice d와 Evo-2 부호 일치 여부** 를 대조한다. NT-v2/DNABERT-2는 공개 자산에 **원시 은닉상태가 없어 크기 재계산이 불가** 하므로, 논문의 코사인 결과만 인용하고 크기 재현은 "범위 밖(한정)"으로 명시한다.
- **성과**: Evo2 크기 약함/부호 반대(d=+0.174 vs 코사인 −0.099). HyenaDNA에서 곡률(궤적) 생물학 신호 재현(d=−0.138, 부호 일치). NT-v2/DNABERT-2는 공개 자산으로 크기 재계산 불가(범위 한정). → per-bp causal-LM 계열 내 재현(Evo-2 특이 아님), 단 크기 축퇴 교차검증은 미완.

### 실험 6 — 렌즈 필요성 매트릭스 (종합)
| 렌즈 | 유계 | 참조고정 | well-posed | 생물학 d | chr22→chr17 |
|---|:--:|:--:|:--:|--:|--:|
| **코사인/방향** | ✓ | ✓ | ✓ | −0.099 | 105% |
| 크기비 (r−1) | ✗ | ✓ | ✓ | +0.174 | 108% |
| 화이트닝 거리 | ✗ | ✓ | ✓ | −0.155 | 117% |
| 경로 굴곡도 | ✗ | ✗ | ✓ | −0.437 | 97% |
| 궤적(속도+곡률) | ✗ | ✗ | ✓ | **−0.889** | 90% |
- **필요성/목적**: "왜 코사인, 왜 다른 렌즈가 아닌가"를 한 표로.
- **진행 방법**: 새 계산 없이 **Stage-6 결과 CSV를 취합** 한다 — `splice_vs_intron.csv`(렌즈별 생물학 d), `chr17_replication/retention_table.csv`(chr22→chr17 전이율·부호 보존), `variant_settling_cells/cell_auroc.csv`(렌즈별 정착 AUROC). 5개 렌즈(M1 코사인/M2 크기/M4 화이트닝/M5 굴곡도/M3 궤적)를 **6개 기준(유계·참조고정·well-posed·생물학 d·전이·AUROC)** 으로 한 표에 채점하며, **각 렌즈의 강점과 약점을 같은 표에 함께** 적어(불리한 셀을 숨기지 않음) 편향을 배제한다.
- **성과(정직)**: 코사인은 **최강 생물학 판별자가 아니다**(궤적이 −0.889로 압도). 그러나 궤적/굴곡도는 **참조가 없어 "출력 프레임 커밋"을 못 재고**, 크기/화이트닝은 유계·well-posed 위배. **코사인만 R1·R2·R3 전부 충족 = 유일한 구성 정합 렌즈.** (주장 A 종합)

### 실험 7 v2 — 기능적 커밋 / 프록시
- **필요성**: "`h_norm` 기준이 기능적인가", "코사인이 무엇의 대리인인가"를 밝혀야 한다.
- **목적**: readout 스케일 불변(7A), 코사인의 해상도, 궤적과의 차이를 본다.
- **진행 방법**: Evo-2를 윈도우별로 forward(메모리 절약을 위해 **층별 거리행렬만 CPU로 이동**, 원시 텐서는 스트리밍). **7A**: 한 층의 은닉상태 h와 **α·h(α∈{0.1,0.5,2,10,100})** 를 모델 readout `unembed(RMSNorm(·))`으로 디코딩해 두 분포의 **JS** 를 잰다(≈0이면 크기 불변). **해상도**: 코사인·JSD 각각의 running-min 곡선에서 **전이폭(10→90% 하강 층수)·binariness(가장 큰 단일층 하강/총 하강)·보간 정착의 구분 레벨 수** 를 대조한다. **프록시·궤적**: 같은 파이프라인으로 얻은 **코사인 정착층·궤적 정착층을 JSD 정착층과 Spearman** 으로 비교하고, 궤적 정착의 **표준편차(상수 여부 감사)** 를 기록한다.
- **성과**: **7A: readout JS = 2.8×10⁻⁵ ≈ 0** → 모델은 크기를 못 읽음(성질 수준의 "왜 크기가 아닌가"). **궤적 디코플링**: 궤적 정착 std=1.475(실제로 변함)인데도 커밋과 상관 0.028 → **진짜 직교**(다른 질문). **해상도**: 코사인이 JSD보다 부드럽고 촘촘(전이폭 14.1 vs 8.8 등)하나 **1.5~1.9× 완만**하고 두 정착 분포 모두 과집중(~99.5%가 한 층). **프록시 충실도 0.869/98.8%** 이나 readout이 방향 기반이라 **부분 순환** → "증명 아닌 프록시"로 강등. → 코사인은 `h_norm`에 대한 각도를 디코딩 없이 재는 **부드러운 대리인**.

### 실험 9 — 차원성·귀무 대조 ★ (왜 32-레이어가 유효한가)
- **필요성**: "32개 레이어를 쓰는 게 그냥 차원(파라미터) 부풀리기 아닌가"에 반드시 답해야 한다.
- **목적**: 라벨 순열 귀무, 차원 곡선, 차원-매칭 대조, 중복성, 레이어 셔플로 32-층 신호의 실재를 검정.
- **진행 방법**: 8,008 변이의 **32층 코사인 특징**으로 5-fold CV AUROC(0.938)를 기준값으로 두고, **5개 대조군을 각각 실제와 비교** 한다 — **H9a** 라벨을 무작위로 섞은 **순열 귀무 분포**(용량만의 효과 제거), **H9b** 상위 k개 주성분만 쓴 **차원 곡선**(신호가 소수 성분에 집중되는지), **H9c** 같은 차원의 **통제 특징**(크기+노이즈, 크기+셔플코사인), **H9d** 32-d vs 최적 단일층·**층간 상관·argmax 엔트로피**(신호가 한 층에 몰렸는지 분산됐는지), **H9e** **레이어-인덱스 32-d vs 순서무시 4-d 요약 vs 레이어-셔플 32-d**(층의 "순서"가 정보인지). 각 대조가 실제보다 낮으면(부트스트랩 CI로) 32층 신호가 실재한다고 판정.
- **성과(모두 PASS)**:
  - **H9a 순열 귀무**: 실제 cos 0.938 vs 라벨섞기 0.500(p=0.024) → **차원 용량만으론 점수가 안 나온다.**
  - **H9b 차원 곡선**: k=1→0.82, k=2→0.90, k=32→0.938; 귀무는 평평 → 실제−귀무 격차 ≥0.327.
  - **H9c 차원-매칭**: 실제 64-d(0.949) > 크기+노이즈(0.927)·크기+셔플코사인(0.930) → **여분 컬럼은 "레이어 정렬된 진짜 코사인"일 때만 도움.**
  - **H9d 중복성**: 크기 32-d ≈ 1-d(argmax 엔트로피 **0.0** = 한 층 고정), 코사인 32-d ≫ 1-d(엔트로피 **1.29 bits** = 분산) → scoring(크기)·localization(코사인) 직교.
  - **H9e 국소성**: 레이어-인덱스 코사인(0.938) > 순서무시 요약(0.866) > **레이어 셔플(0.831)** → **"어느 레이어인가"가 정보.**

### 실험 10 — 인과적 정착 일치 검정 (설계·수정 이력 포함)
- **필요성**: 지금까지의 논거(왜 코사인·왜 방향·왜 레이어·왜 `h_norm`)가 **서로 다른 코호트·파이프라인에 흩어져** 있었다. 이를 **하나의 인과 실험**으로 묶고, 미수행 실험 8(참조 특이성)을 인과 버전으로 대체하는 것이 목표.
- **목적**: 세 커밋 층을 정의해 관계를 본다 — c_cos(방향이 `h_norm`에 도달), c_jsd(예측이 최종에 도달), **c_causal("레이어 ℓ부터 방향 업데이트를 정사영으로 고정해 흘려도 출력이 안 변하는 층")**. 그리고 방향/크기·참조·레이어 대조를 함께 건다.
- **방법론 개선(중요)**: ① freeze를 상태 전체가 아니라 **각 블록 업데이트의 평행 성분만 남기는 "업데이트 정사영"** 으로 바꿔(당신 제안) off-manifold 붕괴·NaN을 제거. ② 크기 대조는 **수직 성분만 남기는 대칭 대조군**. ③ **임계값 τ 스윕 + 분할(held-out) 안정성**으로 "τ 선택이 결과를 좌우하지 않는지" 검정. ④ c_causal은 c_cos와 동일한 **first-crossing**(처음 임계 교차)으로 정의.
- **진행 방법**: gDTR로 Evo-2를 forward하며 블록 hook으로 **업데이트 정사영 freeze** 를 적용한다 — 층 ℓ부터 각 블록의 업데이트 δ를 `u0=unit(x_ℓ)`에 정사영해 **평행 성분만 남기면(=회전 차단)** 방향이 ℓ 값으로 고정되고, **수직 성분만 남기면(=성장 차단)** 크기 대조가 된다. 윈도우별로 세 커밋층을 구한다: **c_cos**(코사인 거리 running-min 정착, 참조=`h_norm`), **c_jsd**(JSD running-min 정착), **c_causal**(방향 freeze-KL을 토큰별 최댓값으로 정규화해 **처음 τ 밑으로 떨어지는 층**). 비교는 세 갈래다 — (A) **방향 freeze vs 크기 freeze** 의 출력 KL 비율(인과 지배), (B) c_causal을 **`h_norm` vs 무작위 vs 중간층** 참조 코사인 정착과 각각 Spearman(참조 특이성), (C) c_causal vs c_cos/c_jsd 를 **τ 스윕(0.02~0.5) + 두 개의 분리된 윈도우 절반(held-out)** 에서 Spearman(일치·안정성). `_diagnostics`로 각 배열의 유한·비상수 여부를 매 실행 기록.
- **성과(실측, real 2560토큰)**:
  - ✅ **방향 인과 지배: `dir/mag = 133×`.** "회전을 막는 개입(방향)"이 "성장을 막는 개입(크기)"보다 출력을 **133배** 더 바꾼다 → 실험 4v2(40~145×)·7A와 정합하는, 더 강하고 대칭적인 인과 증거. (주장 C 강화)
  - ✅ **참조(`h_norm`) 특이성: hnorm 0.158 vs random −0.014, mid −0.016(@τ=0.05).** 인과 커밋 구조가 **오직 `h_norm` 기준 코사인과만** 양의 상관, 무작위·중간층 참조에선 0. → **`h_norm`이 기능적 출력 프레임임을 인과로 뒷받침**(미수행 실험 8의 인과적 대체). (주장 D 강화)
  - ⚠️ **삼중 일치(c_cos = c_causal)는 약함/미성립.** c_causal~cos = 0.16(@τ0.05), 최고 0.27(@τ0.02), τ 전 구간 불안정(0.01까지 하강, 비단조). c_causal~jsd도 유사(0.19@τ0.05). → **"코사인 정착 층이 곧 인과 커밋 층"이라는 정밀 일치는 성립하지 않는다.**
  - (기술 상태) `_diagnostics`상 모든 배열 유한·비상수, cos~jsd=0.854 — 계산 정상. 삼중 일치의 약함은 **버그가 아니라 실제 결과**.

---

## 3. 왜 코사인이 유용하고 합리적인가 (통합 논증 — 5단 사슬)

각 고리는 위 실측으로 뒷받침된다. 사슬에 빈틈이 없도록 순서대로 읽는다.

1. **모델이 읽는 양은 방향이다.** readout `unembed(RMSNorm(h))`은 스케일 불변(7A: JS 2.8×10⁻⁵). 크기는 **구조적으로 버려진다** → 크기 렌즈는 모델이 안 읽는 양을 잰다.
2. **인과적으로도 방향이 지배한다.** 동일 예산 출력공간에서 방향이 크기를 **40~145×(실험4v2), 133×(실험10)** 압도하고 null도 상회. → 방향은 "모델 계산이 실제로 좌우되는" 양.
3. **정착 구성으로는 방향만 well-posed하다.** 크기·거리는 상수/깊이지배·축퇴(실험1·6), 궤적·굴곡도는 참조가 없다(실험6). 코사인만 **유계·참조고정·비축퇴**.
4. **그 참조(`h_norm`)는 기능적이다.** 인과 커밋 구조가 **`h_norm` 기준에서만** 나타난다(실험10: hnorm 0.16 vs random/mid 0). → "출력 준비 프레임에의 도달"이라는 구성의 기준이 임의가 아니라 모델의 실제 출력 프레임임을 인과로 확인.
5. **코사인은 그 구성을 값싸고 부드럽게 잰다.** `h_norm`에 대한 각도를 **디코딩 없이**, JSD보다 부드럽게(실험7) — 즉 **완벽한 대리인(proxy)**.

→ **코사인의 정당성은 "분류 우월"이 아니라 (모델이 읽는 양 = 방향) + (인과적 방향 지배) + (유일한 구성 정합) + (기능적 참조)에 근거한다.** 궤적이 생물학을 더 잘 가르더라도, 그것은 "출력 프레임 커밋"이라는 우리가 물은 질문에 답하지 않는다.

---

## 4. 왜 32-레이어 "차이(누적)" 사용이 좋은가

핵심은 실험 9이며, 세 독립 검정이 동시에 통과한다.

1. **차원 부풀리기가 아니다.** 라벨을 섞으면 AUROC가 0.50으로 붕괴(H9a), 여분 컬럼은 귀무가 무가치함을 보인다(H9b). "32차원이라 잘 맞춘다"가 아니다.
2. **레이어의 순서(인덱스)가 정보다.** 레이어를 섞으면 0.938→0.831(−0.108, H9e). **"몇 번째 레이어에서 방향이 변했는가"** 가 실제 신호다.
3. **코사인의 층위 신호는 진짜 분산돼 있다.** 크기는 argmax 엔트로피 0.0(한 층 고정, 32-d≈1-d) = 어디서 읽어도 같은 스칼라. 코사인은 엔트로피 1.29 bits, 32-d≫1-d = **레이어마다 다른 정보**(H9d).

→ **"32-레이어에 걸친 코사인 차이"는 각 층이 서로 다른, 순서에 의존하는, 라벨과 결부된 정보를 담기 때문에 유효하다.** 이것이 크기(단일 스칼라의 반복)와 본질적으로 다른 점이며, "정착 깊이 = 레이어 인덱스 값"이 생물학적 의미를 갖는 이유다. 실험 10은 여기에 **그 인과 구조가 `h_norm` 프레임에 한정된다**를 더해, 층위 신호가 "출력에의 방향적 커밋"과 결부됨을 인과적으로 시사한다.

---

## 5. 최종 방향성 (한 방향의 메시지)

> **GDTR은 "모델이 각 토큰의 답을 언제 확정하는가"를, 모델이 실제로 읽는 방향으로, 모델 자신의 최종 프레임 `h_norm`에 대한 각도로, 레이어 축을 따라 부드럽게 추적한다.** 크기는 모델이 읽지 않는(스케일 불변) 한 층짜리 단일 스칼라이고, 궤적은 더 강한 판별자지만 출력 커밋이 아닌 기하를 잰다. 코사인만이 (1) 모델이 읽는 양을, (2) 기능적으로 검증된 `h_norm` 참조에 대해, (3) 유계·well-posed 정착으로, (4) 순서·라벨·인과에 결부된 레이어별 방식으로 측정한다. 실험 10은 **모델 계산이 크기 아닌 방향에 133배 인과적으로 민감하고 그 구조가 오직 `h_norm`에서만 나타남**을 보여, "왜 코사인·왜 `h_norm`"을 인과로 확정한다. 다만 "코사인 정착 층이 곧 단일 인과 커밋 층"이라는 정밀 일치는 (아래 이유로) 약한 상관까지만이다.

---

## 6. 반론 지도 (실험이 함께 답한다)

| 반론 | 답한 실험 | 결론 |
|---|---|---|
| "크기가 분류를 이기는데 왜 코사인?" | 3b, 6, 9(H9d) | 이 데이터선 동률(0.938 vs 0.935); 크기=단일 스칼라, 코사인=국소화. 분업. |
| "모델이 정말 방향을 쓰나?" | 4v2, 7A, **10** | 동일예산 출력공간 방향 40~145×(4v2)·**133×(10)** > 크기; readout은 크기 불변. |
| "`h_norm` 기준이 특별한가?" | 7, **10** | **인과 커밋 구조가 `h_norm`에서만(hnorm 0.16 vs random/mid 0)** → 기능적 프레임(실험10). |
| "궤적이 더 센데 왜 안 씀?" | 6, 7 | 궤적은 참조 없음·기능적 커밋과 직교(0.028) = 다른 질문. |
| "32-레이어가 차원 부풀리기 아닌가?" | 9 | 순열귀무 0.50, 셔플 −0.108, 크기≈1-d vs 코사인 분산. 유효. |
| "코사인이 소수 이상 차원 산물 아닌가?" | 2(부분) | L29 깨끗(0.76%). **중간층 미해결(§8).** |

---

## 7. 우리가 원했던 결론에 얼마나 부합했나 (정직한 점검)

| 원했던 주장 | 결과 | 강도 |
|---|---|---|
| A 크기/거리는 정착 렌즈로 부적합 | 크기 상수·축퇴, 코사인만 well-posed(1,6) | **강함** |
| B 방향=국소화 / 크기=스코어링 (분업) | 동률·상보, 코사인 분산·크기 1-스칼라(3b,9) | **강함** |
| C 방향의 인과 지배 | 40~145×(4v2), **133×(10)** | **강함** |
| D `h_norm` 참조가 기능적 | 인과 커밋이 `h_norm`에서만(10), 7A/프록시(7) | **중~강** |
| E 32-레이어 누적이 유효 | 순열·셔플·중복성 3중 통과(9) | **강함** |
| (부가) 코사인 정착 = 단일 인과 커밋 층 | 약한 양의·`h_norm` 특이 상관(≤0.27, τ불안정) | **약함(미성립)** |

→ **핵심 6개 중 5개(A~E)는 강하게 성립**하며, 이들이 "왜 코사인·왜 32-레이어"의 논거다. 마지막 정밀 일치만 약하다.

---

## 8. 아쉬운 점(한계)과 실험 10의 발전 방향

### 8.1 아직 약하거나 미완인 것
1. **삼중 일치(c_cos = c_causal)가 약함(≤0.27, τ불안정).** 원인은 버그가 아니라 세 가지다: (a) **두 지표가 다른 사건을 잰다** — c_cos는 raw 방향의 *중간(mid) 접근*을, c_causal은 *회전 차단의 출력 충분성*을 재는데, Evo-2는 L29까지 방향이 `h_norm`과 거의 직교였다가 **L30에서 보편적으로 회전**해 스냅한다(freeze 곡선의 후반 스파이크). (b) **정직한(온건한) 업데이트 정사영일수록 신호가 옅어진다** — 예전 full-norm 방식이 0.33까지 나온 건 off-manifold 과대개입의 부작용이었다. (c) **c_cos 자체가 과집중(~99.5%가 한 층, 실험 7)** 이라 어떤 지표와도 상관 상한이 눌린다.
2. **실험 2 중간층 로그 차원.** L15 상위 8차원 96% 지배 + 표준화 강건성(H2b/H2c) 미실행. "코사인이 진짜 다차원 방향을 읽는다"는 표준화 후에도 정착·생물학이 유지됨을 보여야 완결.
3. **정착 분포 과집중.** q70@L28·W=3 보정이 정착을 거의 상수로 만들어 해상도·판별력·상관을 동시에 제약. → **γ 재보정(q40~q70 스윕) 또는 연속 running-min 곡선** 사용이 원리적 지렛대.
4. **실험 4v2 소표본(n=23)·실험 5 크기 교차검증 미완.**

### 8.2 실험 10의 발전 방향 (논리 유지하며 정밀 일치를 되살리는 길)
- **(1) c_cos의 분산을 먼저 넓혀라(가장 원리적).** 과집중이 상관 상한을 누르므로, γ를 재보정하거나 정착을 **연속 곡선(running-min 면적/최급강하 층)** 으로 정의하면, c_causal과의 상관 자체가 올라갈 수 있다. 이건 fitting이 아니라 실험 7이 이미 지적한 결함의 수정이다.
- **(2) 회전(L30)을 분리하라.** 후반 보편 회전이 c_causal을 오염시키므로, **freeze 후보 층을 pre-rotation 범위(ℓ≤27)로 제한**하면 c_causal이 "보편 회전"이 아니라 *토큰별 pre-rotation 커밋*을 재게 되어 c_cos와 정렬될 여지가 커진다. (코드에 `--max-freeze-layer` 옵션으로 즉시 추가 가능.)
- **(3) "단일 커밋 층" 가정을 완화하라.** freeze 곡선이 **이중 구조(중간 접근 + 후반 회전)** 임이 실측으로 드러났다. 커밋을 단일 스칼라가 아니라 **이중 커밋(early/late)** 으로 모델링하고, c_cos를 그중 *중간 접근*과만 비교하는 것이 현상에 더 충실하다.
- **(4) 무엇을 주장으로 확정할지 정직하게 고정하라.** 상관을 억지로 끌어올리려 정의를 계속 바꾸는 것은 fitting이다. **강한 결과(방향 인과 지배 133×·`h_norm` 참조 특이성)를 실험 10의 확정 성과로** 삼고, 정밀 일치는 "약한 양의·`h_norm` 특이 상관"으로 정직하게 보고하는 것이 과학적으로 옳다.

### 8.3 한 줄 총평
> 실험 1~10은 **"모델은 크기가 아니라 방향(회전)에 인과적으로 좌우되고(133×), 그 방향은 모델 자신의 출력 프레임 `h_norm`에 대한 각도로 재야 하며(참조 특이성), 그 정착이 일어나는 32-레이어 인덱스는 순서·라벨·내용에 결부된 실재 신호(차원 부풀리기 아님)"** 임을 강하게 확립했다. 이것이 코사인을, 그리고 레이어 누적을 쓰는 정당한 이유다. 남은 과제는 "코사인 정착 층 = 단일 인과 커밋 층"이라는 **정밀 일치**를 c_cos 분산 확대·회전 분리로 되살리는 것이며, 그것이 실험 10의 다음 단계다.
